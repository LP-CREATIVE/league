{
  "name": "league-coach",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "prisma:generate": "pnpm --filter @league/db prisma generate"
  },
  "devDependencies": {
    "@prisma/client": "^5.15.0",
    "@types/node": "^20.12.8",
    "eslint": "^8.57.0",
    "prettier": "^3.3.1",
    "prisma": "^5.15.0",
    "turbo": "^2.0.6",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "tsx": "^4.20.3"
  }
}
